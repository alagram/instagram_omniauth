%header.main-header
  %a.logo(href="/")
    Instagram Omniauth
  .nav.navbar.navbar-static-top(role="navigation")
    .navbar-custom-menu
      %ul.nav.navbar-nav
        %li.dropdown.user.user-menu
          %a.dropdown-toggle(data-toggle="dropdown" href="#")
            %img.user-image(alt="(User Image)" src="#{current_user.profile_picture}")
            %span.hiddle-xs= current_user.name
          %ul.dropdown-menu
            %li.header
              = current_user.name
            %li.divder
            %li
              = link_to destroy_user_session_path, method: :delete do
                %i.fa.fa-sign-out
                  Log out
